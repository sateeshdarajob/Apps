import { RoutePlaceholderPage } from '../shared/RoutePlaceholderPage';

export function SettingsPage() {
  return (
    <RoutePlaceholderPage
      title="Settings"
      description="User preferences, saved filter views, and org configuration will live here."
    />
  );
}
