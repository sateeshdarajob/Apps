import { RoutePlaceholderPage } from '../shared/RoutePlaceholderPage';

export function OverviewPage() {
  return (
    <RoutePlaceholderPage
      title="Overview"
      description="Portfolio health overview, executive summary KPIs, and cross-program signals will appear here. Charts and widgets are intentionally deferred to the next increment."
    />
  );
}
