import { RoutePlaceholderPage } from '../shared/RoutePlaceholderPage';

export function DecisionsPage() {
  return (
    <RoutePlaceholderPage
      title="Decisions"
      description="Decision log, ADR links, and pending executive decisions will live on this page."
    />
  );
}
