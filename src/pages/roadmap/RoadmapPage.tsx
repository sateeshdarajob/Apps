import { RoutePlaceholderPage } from '../shared/RoutePlaceholderPage';

export function RoadmapPage() {
  return (
    <RoutePlaceholderPage
      title="Roadmap"
      description="Program milestones, quarter roadmap, and milestone RAG status will be built in a later increment."
    />
  );
}
