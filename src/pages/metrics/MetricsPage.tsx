import { RoutePlaceholderPage } from '../shared/RoutePlaceholderPage';

export function MetricsPage() {
  return (
    <RoutePlaceholderPage
      title="Metrics"
      description="Engineering and program metrics dashboards will be added in a subsequent release."
    />
  );
}
